(def name (input "What is your name?"))
(def age  (input "How old are you?"))

(def welcome (concat "Hello, "
                     name
                     "! "))

(print welcome)

(print (concat "You're so "
               (if (>= age 18)
                   "old!"
                   "young!")
               "!"))
